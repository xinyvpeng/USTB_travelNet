# TravelNet 环境变量配置
# 复制此文件为 .env.local 或直接在构建环境中设置

# 认证密码（必需）
# 设置一个安全的密码，用于登录以进行编辑操作
VITE_AUTH_PASSWORD=your_secure_password_here

# 注意：在生产部署中（如GitHub Pages），需要通过构建环境设置此变量
# GitHub Actions示例：在仓库的Settings > Secrets and variables > Actions 中添加VITE_AUTH_PASSWORD